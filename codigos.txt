# 1) Crear
python -m venv .venv

# 2) Activar
.\.venv\Scripts\Activate.ps1

# 3) Actualizar pip y deps
python -m pip install --upgrade pip
pip install -r requirements.txt  # si lo tienes

# 4) Verificar
python -V
pip -V

# 5) Desactivar
deactivate

####PARA GENERAR LAS LISTAS DE AudIOS
py .\preprocesing\gen_splits.py --dataset-dir dataset-audio-raw --dev-size 80 --test-size 0


### en hydraa
cd /workspace1/hugoz/Audio_procesing/
source .venv/bin/activate

####
ejecutar en hydraa# 1) Crear y activar venv en raíz si no existe
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt   # o tus deps

sh# 2) Ejecutar
bash pipeline_all_in_one.               # sin ingesta
INGEST=1 bash pipeline_all_in_one.sh      # con ingesta


git pull --ff-only


scp hugoz@kraken.ing.puc.cl:./metrics.csv .
python .\graficar.py ".\metrics.csv" ".\"

cd artifacts/
cp metrics.csv ../
cp metrics.csv /mnt-homes/kraken/hugoz/metrics.csv


probsr compute metrics

python enh/compute_metrics.py --dataset-dir dataset-audio-raw --out-csv artifacts/metrics.csv